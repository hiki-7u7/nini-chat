import { FC } from "react";

interface ActionTooltipProps {
  label: string,
  side: 'top' | 'bottom' | 'left' | 'right',
}
 
const ActionTooltip: FC<ActionTooltipProps> = ({ label }) => {
  return (
    <>
    </>
  );
}
 
export default ActionTooltip;